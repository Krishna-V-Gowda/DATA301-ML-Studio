# Contributing

Keep algorithms framework-independent under `lib/ml`; laboratory components should render and explain them rather than reimplement them. Add a worked example and, where meaningful, a metamorphic property. Run `npm run verify:source`, type checking, and the production build before opening a pull request. Changes to authentication, RLS, signed URLs, or redirect validation require an explicit security rationale.
